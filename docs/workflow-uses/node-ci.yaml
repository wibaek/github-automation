name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  node-ci:
    name: Node.js CI
    uses: wibaek/github-automation/.github/workflows/node-ci.yaml@v1.1.0
    with:
      node-version: "22"
      package-manager: "pnpm"
      pnpm-version: "10"
      lint-command: "auto"
      typecheck-command: "auto"
      test-command: "auto"
      build-command: "auto"
      cache-dependency-path: "pnpm-lock.yaml"
